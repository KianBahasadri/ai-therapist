[project]
name = "ai-therapist"
version = "0.1.0"
description = "Local FastAPI prototype for AI therapy research workflows."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.115.6",
    "httpx==0.28.1",
    "pydantic-settings==2.7.0",
    "uvicorn[standard]==0.32.1",
]

[tool.uv]
package = false
